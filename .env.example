# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finance_db
DB_USERNAME=finance_user
DB_PASSWORD=finance_pass

# Security Configuration
# JWT_SECRET must be at least 512 bits (64 characters) long for HS512
JWT_SECRET=your_super_secret_jwt_key_that_is_at_least_64_characters_long_for_hs512_to_work_correctly
